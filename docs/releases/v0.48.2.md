# CodexBar 0.48.2 — 已验收的并发检测与悬停结果

用户于 2026-09-22 确认验收通过。此发布提供原样保存的 Windows x64 Release 便携版，以及对应源码快照，不是安装包或全平台兼容认证。

## 下载与使用

下载附件 `CodexBar-0.48.2-portable.exe`。这是未签名程序，Windows 可能提示发布者未知；请核对下方哈希。需要 Windows 和 WebView2 Runtime；先从旧版菜单选择“退出”，再启动新版，不要同时运行不同路径的版本。

文件版本 0.48.2，构建号 92。SHA256：

`B3940EE3E5665A5DE230B8DD3E357C1571B154739116F002F5A950560C48511F`

## 本次变化

- Kimi、MiniMax、GLM 的统一并发检测，覆盖已启用供应商的全部启用 Key。主窗口按钮在“刷新全部”左侧。
- 点击直接检测，不再展开二次确认、模型输入框或 Key 清单；点击可能消耗套餐额度或余额。每个 Key 最多两条请求、不自动重试，支持取消。
- 各供应商失败计数旁提供“并发检测结果”；鼠标悬停显示本供应商结果，移入卡片保持显示，离开后隐藏，长列表支持滚动。
- 明确受限的 Key 显示“并发受限”。普通限流、额度或权限错误不会被当作并发限制。客户端响应重叠不等于测得服务端真实并发上限。
- 修复相同路径、相同文件且单实例身份可验证时的便携版重复启动交接；不同路径、不同内容或未知身份仍安全拒绝自动接管，不自动结束旧进程。

## 验证与边界

前端 360 项测试通过；最近完成的核心、命令入口和桌面测试共 1,495 项通过。类型检查、版本一致性与 Release EXE 编译通过。用户已验收本版本。

检测结果仅在当前进程内保留，退出后清除；GPT/Codex 等其他供应商暂未适配；取消不保证服务端立即停止计费。更新源保持未配置。

真实安装/升级/卸载、历史配置跨机器迁移及完整旧新进程矩阵仍待验证；PTY 历史间歇失败未宣称根治。另记录闲时轮询、多窗口准备竞争、检测后重命名标签不同步三个非阻断改进项。详情见仓库 `docs/known-issues-0.48.2.zh-CN.md`。

不会上传个人配置、API Key、日志或编译缓存。此附件不是对所有套餐、模型和其他电脑的可用性保证。
